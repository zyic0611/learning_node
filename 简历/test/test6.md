### 第一部分：高并发与分布式架构场景拷问

**1. Redis 预扣减与 MQ 消息丢失的“悬案”** 你在简历中提到：“引入 Redis + Lua 脚本实现任务预扣减原子操作……采用 RabbitMQ 异步投递抢单记录”。

- **极端场景：** 假设一个环卫工人点击抢单，你的 Lua 脚本在 Redis 中成功扣减了任务名额，但紧接着 JVM 突然宕机（或者 OOM 直接被系统 Kill），导致后续的 RabbitMQ 抢单消息**根本没有发出去**。
- **问题：** 此时 Redis 里的名额少了，但数据库里根本没有生成对应的抢单记录。你怎么发现并修复这种不一致？你的架构中是否有针对这种“半路夭折”请求的兜底补偿机制？

A：为了保证消息可靠性投递 开启了发布确认机制 消息成功发送 会回复java客户端一个ack。所以我在发送的时候，判断，在多久内没收到ack 则认为是消息发送失败，则在redis里回滚库存。删除一人一单标记。【错误 这个只能解决网络抖动导致的发送失败。

**如果是JVM突然宕机或者被kill 也就是刚执行完redis内存扣减 还没来得及调用发送消息【rabbitTemplate.send JVM就崩溃了 confrim机制都没来得及出发。**

**必须引入独立于当前业务流程的对账/补偿机制。比如使用定时任务 每隔5分钟扫描redis中所以预扣减的标记【或者独立的redis hash记录表 与mysql里的真实订单做比对。**

**如果redis扣了名额，但是mysql里超过10min没有对应的枪弹记录 ，说明是个死锁/死亡名额 则由定时任务回滚redis。**

**2. 乐观锁 CAS 与并发冲突** 简历提到：“运用数据库状态机（乐观锁 CAS）保证逆向回滚接口的绝对幂等性”。

- **问题 1：** 请写出你这个业务场景下，执行 CAS 状态流转的**核心 SQL 语句**大概长什么样？

- **问题 2：** 如果在某个极端并发回滚场景下，大量线程同时去 `UPDATE` 同一条记录，导致大量 CAS 失败（自旋开销剧增）。这种情况下数据库层面的压力会极大，吞吐量也会断崖式下跌，你会如何优化或缓解这个问题？

  A1:

  update from tablexxx set status='已支付 where id=xxx and status='未支付';

  A2:上分布式锁 redisson 保证自由一个线程可以update记录 。 或者在redis里做前置保护 一条记录被update则打上标记在redis里 其他线程看到redis里的标记 则停止update操作。

**3. JUC 线程池与延迟双删** 你提到：“基于线程池实现 500ms 异步延迟双删”。

- **基础拷问：** 假设你的自定义线程池配置如下：核心线程数 `corePoolSize = 5`，最大线程数 `maximumPoolSize = 10`，阻塞队列 `workQueue` 容量为 50。当系统瞬间涌入 **60 个**并发的延迟双删任务时，线程池底层会如何分配和处理这些任务？
- **架构设计：** 为什么要用 Java 本地的线程池来做延迟双删？相比于把这个延迟任务交给 **RabbitMQ 的延迟队列（或死信队列机制）**，本地线程池方案在生产环境（比如服务重启、宕机）中存在什么致命缺陷？

A1:首先 前5个任务交给核心线程处理  然后50个任务在阻塞队列排队 然后5个任务 线程池会扩充5个线程来执行则5个任务

A2: 

为什么用本地： 实现轻量化 不用引入中间件 没有网络I/O开销 代码复杂度低

缺陷：存放在线程池里的任务 都是在jvm内存里 如果在500ms 延迟等待期间 服务器进行重启 发布  or宕机  任务丢失 则第二次删除redis操作永远不会执行 变成脏数据。

由rabbitmq的死信队列执行 消息可以持久化道磁盘 不怕服务器重启。

Q：如果在你的自定义线程池中，那排队的 50 个延迟双删任务在执行过程中，某行代码由于 Bug 抛出了未被 `try-catch` 捕获的 `RuntimeException`，处理这个任务的工作线程会直接崩溃被销毁，还是会被线程池默默回收复用？

A：由于并不需要返回值，所以一般选择excute去提交延迟双删任务。 当遇到未捕获的异常的时候 当前的工作线程会直接被销毁，异常会打印在堆栈中 如果线程池工作线程数目少于核心线程数 或者阻塞队列里还有任务 则直接创建出一个新的线程去接受任务。

**4. JVM 生产级故障排查** 你的简历上写了熟悉 JVM 内存模型与 GC 机制。

- **场景题：** 你的“智慧城市环卫系统”在早高峰遭遇了预期外的大规模流量，运维监控大屏突然报警：你的核心订单服务 **CPU 飙升持续在 100%，并且频繁触发 Full GC，接口开始大面积超时**。
- **排查链路：** 请详细描述你接到报警后的排查思路。你会使用哪些 JDK 自带的命令行工具（或者 Arthas 等诊断工具）去定位是哪行代码导致的问题？

A：cpu飙升100%  

此时使用命令top 先排查出占用cpu最大的进程 pid

再使用 top -Hp pid 排查出该进程中占用cpu最大的线程号 tid

然后使用 printf("%x" tid)转成16进制 

再jstack pid |grep -A 20 16进制td 导出该进程的堆栈 搜索线程 打印出前后20行日志 

此时日志中会出现具体的代码行数 定位到java代码里 去里面排查

或者使用阿里开源软件arthas 。先运行arthas 把arthas挂载到出问题的进程上。thread -n 3打印出占用cpu最高的3个线程 。

如果发现是由于频繁的fullgc引发的cpu飙升 那么系统并不是在疯狂计算 而是内存爆了。此时就需要使用jmap -dump导出内存快照 使用MAT内存分析工具分析。

------

### 第二部分：手撕核心算法 (Algorithm)

在多维检索、热榜维护以及中间件底层原理中，经常需要对多个有序集合进行合并。这是一道大厂极为高频的 Hard 级别手撕题，要求不仅要写出代码，还要保证最优的时间复杂度。

**题目：合并 K 个升序链表 (Merge k Sorted Lists)**

**要求：** 给你一个链表数组，每个链表都已经按升序排列。 请你将所有链表合并到一个升序链表中，返回合并后的链表。

多路归并排序 小顶堆